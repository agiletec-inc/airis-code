// Channel commands removed - not needed for AIRIS Code
export const channelCommand = {
  command: 'channel',
  describe: 'Channel commands (not available)',
  handler: () => { console.log('Channel commands are not available in AIRIS Code'); },
};
