// Qwen OAuth UI removed - stub for compatibility
import React from 'react';
import { Text } from 'ink';

type DeviceAuthorizationData = any;

export function QwenOAuthProgress(_props: Record<string, any>) {
  return React.createElement(Text, null, 'OAuth not available');
}
